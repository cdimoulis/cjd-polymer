{{#if data.ripple}}
<paper-icon-button icon="{{display.icon}}" class="{{display.button_color}} {{display.icon_color}}" {{display.disabled}}>
{{else}}
<paper-icon-button icon="{{display.icon}}" noink class="{{display.button_color}} {{display.icon_color}}" {{display.disabled}}>
{{/if}}
</paper-icon-button>

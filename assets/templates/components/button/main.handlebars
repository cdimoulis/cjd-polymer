<paper-button class="{{display.classes}} {{display.extra_classes}}" {{{display.attrs}}} {{{display.extra_attrs}}}>
  {{#if display.icon}}
  <iron-icon icon="{{display.icon}}">
  </iron-icon>
  {{/if}}
  {{display.text}}
</paper-button>

<paper-button class="{{display.classes}}" {{{display.attrs}}} id='{{display.id}}'>
  {{#if display.icon}}
  <iron-icon icon="{{display.icon}}">
  </iron-icon>
  {{/if}}
  {{display.text}}
</paper-button>

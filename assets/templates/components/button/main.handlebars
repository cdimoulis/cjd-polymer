{{#if display.raised}}
<paper-button raised class="{{display.button_color}} {{display.text_color}}">
{{else}}
<paper-button>
{{/if}}
  {{#if display.icon}}
  <iron-icon icon="{{display.icon}}">
  </iron-icon>
  {{/if}}
  {{display.text}}
</paper-button>
